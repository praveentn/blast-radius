# blast-radius
Score what an AI agent could destroy before anyone can stop it.  Most agent security checklists audit the tools you granted. The expensive incidents come from a credential the agent **found**. This scans both.  ``` declared   the tools you handed it            (--manifest) ambient    the credentials it can reach       (the working tree, --env) ```
